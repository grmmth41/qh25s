import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.azota.vnu1001',
  appName: 'Azota VNU1001',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
